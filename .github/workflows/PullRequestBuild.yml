name: Continuous Integration Pull Request

on:
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
        source-url: https://nuget.pkg.github.com/wouterfennis/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
    - name: Restore dependencies Api
      run: dotnet restore ./src/Cifra.Api/Cifra.Api.csproj
    - name: Build project
      run: dotnet build ./src/Cifra.Api/Cifra.Api.csproj --no-restore --configuration Release
    - name: Restore dependencies Api Client
      run: dotnet restore ./src/Cifra.Api.Client/Cifra.Api.Client.csproj
    - name: Build project
      run: dotnet build ./src/Cifra.Api.Client/Cifra.Api.Client.csproj --no-restore --configuration Release
    - name: Pack
      run: dotnet pack ./src/Cifra.Api.Client/Cifra.Api.Client.csproj --no-build --configuration Release --version-suffix ${{GITHUB.RUN_ID}}
    - name: Publish
      run: dotnet nuget push "./src/Cifra.Api.Client/bin/Release/Cifra.Api.Client.*.nupkg" --source https://nuget.pkg.github.com/wouterfennis/index.json --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
    - name: Build and push
      uses: docker/build-push-action@v3
      with:
          context: .
          file: ./src/Dockerfile.Frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-frontend:${{GITHUB.RUN_ID}}
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-frontend:dev
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-frontend:latest
    - name: Build and push
      uses: docker/build-push-action@v3
      with:
          context: .
          file: ./src/Dockerfile.Api
          push: true
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-api:${{GITHUB.RUN_ID}}
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-api:dev
            ${{ secrets.DOCKER_HUB_USERNAME }}/cifra-api:latest