name: Continuous Integration Pull Request

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
        source-url: https://nuget.pkg.github.com/wouterfennis/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
    - name: Restore dependencies of project
      run: dotnet restore
    - name: Build project
      run: dotnet build --no-restore --configuration Release
    - name: Run tests
      run: dotnet test --no-restore
    - name: Bump version and push tag
      id: tag_version
      uses: mathieudutour/github-tag-action@v5.3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Publish project for Linux x64
      run: dotnet publish ./src/Cifra.ConsoleHost/Cifra.ConsoleHost.csproj --configuration Release --no-restore -p:PublishProfile=Linux64
    - name: Upload Linux x64 Artifact
      uses: actions/upload-artifact@v2
      with:
        name: Linux64
        path: |
          ./src/Cifra.ConsoleHost/bin/Release/net5.0/publish/Cifra.ConsoleHost
          ./src/Cifra.ConsoleHost/bin/Release/net5.0/publish/appsettings.json
    - name: Publish project for Windows x64
      run: dotnet publish ./src/Cifra.ConsoleHost/Cifra.ConsoleHost.csproj --configuration Release --no-restore -p:PublishProfile=Windows64
    - name: Upload Windows x64 Artifact
      uses: actions/upload-artifact@v2
      with:
        name: Windows64
        path: |
          ./src/Cifra.ConsoleHost/bin/Release/net5.0/publish/*.exe
          ./src/Cifra.ConsoleHost/bin/Release/net5.0/publish/appsettings.json